Pasos a ejecutar para poder usarlo...
crear un entorno virtual:
linux: python3 -m venv venv
windows:python -m venv venv

activar entorno virtual:
ubuntu: source venv/bin/activate
windows: venv\Scripts\activate

instalar requerimientos:
pip install -r requirements.txt

Ejecutar migraciones:
python manage.py migrate

__RECUERDA PRENDER XAMPP__

comando para ejecutar el encriptador de contraseñas
python Salvador/migrar_contrasenas.py

iniciar el server:
python manage.py runserver

iniciarlo para movil:
python manage.py runserver 0.0.0.0:8000


--prender conexion lan para movil--
# 1. Crear hotspot
Linux:  nmcli device wifi hotspot ifname wlp0s20f3 ssid "RBE-local" password "12345678"





--en dado caso la api de elipse este caida pueden ir a esta pagina
https://console.groq.com/

-crean su cuenta le dan a apikeys crear api-key

y pueden crear su propia api-key para luego ponerla en el archivo .env en
GROQ_API_KEY=


 